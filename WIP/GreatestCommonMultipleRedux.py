# The objective of this program is to generate a program that can take any 2 numbers and deliver the greatest
# common multiple

def get_prime_list(largest):
    a = 0
    boolean_list = [0] * largest
    print(boolean_list)
    # while a <= largest
    #    for
    #    count = 0
    #    boolean_list(count * a) = not boolean_list(count * a)


get_prime_list(5)
